新闻传播学论文文献综述全景指南与深度实战手册
项目说明：本手册针对新闻传播学（学硕/专硕/博士）学术论文写作，提供专著级深度指导。手册采用模块化文件架构，点击下方章节标题或目录链接，即可直接跳转至对应章节的完整细化内容（含高密度理论阐述、实操表格、写作用例与 Mermaid 动态思维导图）。

一、 全景知识图谱与防遮挡架构
[代码框开启-mermaid]
graph TD
Root["新闻传播学文献综述全景指南"] --> C1["01-认知篇"]
Root --> C2["02-检索与管理篇"]
Root --> C3["03-梳理与批判篇"]
Root --> C4["04-结构与写作篇"]
Root --> C5["05-学科专项篇"]
Root --> C6["06-前沿拓展篇"]

C1 --> C1_1["学术本原与四大功能"]
C1 --> C1_2["四大元认知检验 Q1-Q4"]
C1 --> C1_3["八大常见误区与矫正矩阵"]
C1 --> C1_4["七大落地应用场景"]

C2 --> C2_1["中外文顶级期刊矩阵"]
C2 --> C2_2["三级文献筛选法"]
C2 --> C2_3["结构化文献矩阵表"]
C2 --> C2_4["Zotero 管理 SOP 与布尔检索"]

C3 --> C3_1["学术合成 vs 文献堆砌"]
如果使用标准的 XML/HTML 标签结构，前端渲染引擎在解析纯文本时，可能会直接把 <GenerateWidget component_placeholder_id="GenerateWidget_c_db1d4668b3494859_r_b322e8595a2e1d42_0"> 等自定义标签识别为未知的 HTML 元素。一旦浏览器或 Markdown 解析器认为它是 HTML 标签，就会尝试将其隐去，导致页面出现“一片空白、什么都渲染不出来”的情况。

为了确保前端能够正确解析并渲染动态组件（而不是把它当成普通 HTML 标签吃掉），可以从以下两个角度来解决：

1. 代码块与格式要求
前端的 Markdown 解析引擎（如 react-markdown 或自定义 Parser）通常依赖特定的声明方式。如果必须放在代码块中，请确保指定了正确语言标识符（如 json 或 xml），例如：

Markdown
```json
{
  "widgetSpec": {
     "height": "600px",
     "prompt": "**Objective:** 创建一个交互式图表...\n**Data State:** ...",
     "initialValues": {}
  }
}
```
2. 前端解析与渲染逻辑检查（如果是你在开发前端）
如果你正在开发或调试这个 AI 的前端应用，渲染空白通常是由于以下解析逻辑断层导致的：

转义字符问题：如果输入的文本中包含 < 或 >，Markdown 解析器会将其视为未闭合的 HTML 标签。在解析流（Stream）中，需要将这类标签捕获并交给自定义的 React / Vue 组件渲染，而不是直接插入 dangerouslySetInnerHTML。

自定义 Block 匹配正则：检查解析逻辑是否成功匹配到了标签。常见的匹配模式应该识别开头的 <GenerateWidget component_placeholder_id="GenerateWidget_c_db1d4668b3494859_r_b322e8595a2e1d42_1"> 和结尾的 </GenerateWidget>，并将其提取为 JSON 字符串进行 JSON.parse()。
