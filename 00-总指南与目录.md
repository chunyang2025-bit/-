新闻传播学论文文献综述全景指南与深度实战手册
项目说明：本手册针对新闻传播学（学硕/专硕/博士）学术论文写作，提供专著级深度指导。手册采用模块化文件架构，点击下方章节标题或目录链接，即可直接跳转至对应章节的完整细化内容（含高密度理论阐述、实操表格、写作用例与 Mermaid 动态思维导图）。

一、 全景知识图谱与防遮挡架构
代码段
graph TD
    Root["新闻传播学文献综述全景指南"] --> C1["01-认知篇"]
    Root --> C2["02-检索与管理篇"]
    Root --> C3["03-梳理与批判篇"]
    Root --> C4["04-结构与写作篇"]
    Root --> C5["05-学科专项篇"]
    Root --> C6["06-前沿拓展篇"]

    C1 --> C1_1["学术本原与四大功能"]
    C1 --> C1_2["四大元认知检验 Q1-Q4"]
    C1我明白你的意思了！这并不是 Markdown 语法写错或者内容没发全的问题，而是**很多 Markdown 渲染器（如标准的 GitHub Markdown、部分笔记软件或 Markdown 预览插件）默认是不支持自定义 HTML 标签或扩展 XML 标签（如 `<Carousel>` 或 `<Image>`）的**。

当渲染器遇到它不认识的标签时，通常有两种处理方式：
1. **静默忽略/过滤**：直接把标签及其包裹的内容隐藏掉。
2. **当作纯文本/原样标签**：只读取标签内符合标准 Markdown 语法的元素（所以你只看到了一张标准 `![...](...)` 格式的图和一段文字）。

如果你需要让内容在**任何通用的 Markdown 编辑器/渲染器**（如 Obsidian、Typora、Notion、GitHub 等）中都能正常显示全部图片和排版，**必须使用纯粹的 Standard Markdown 语法**，而不是扩展组件标签。

---

### 解决方案：使用纯 Standard Markdown 替换

如果你希望显示多张图片和结构化内容，请直接使用下面的标准语法：

#### 1. 多图展示（替代 `<Carousel>`）
标准的 Markdown 没有“轮播图”的概念，通常采用**图片列表**或**并排图片**的形式展示：

**方式 A：图片纵向列表（最兼容）**

![图片1描述](https://example.com/image1.jpg)
*图1：说明文字*

![图片2描述](https://example.com/image2.jpg)
*图2：说明文字*

![图片3描述](https://example.com/image3.jpg)
*图3：说明文字*

**方式 B：HTML 结合（大部分渲染器支持并排）**

<div align="center">
  <img src="https://example.com/image1.jpg" width="30%" />
  <img src="https://example.com/image2.jpg" width="30%" />
  <img src="https://example.com/image3.jpg" width="30%" />
</div>

---

#### 2. 步骤流程（替代 `<Sequence>` 或 `<Step>`）
使用标准的**有序列表**加上**加粗标题**：

1. **第一步：配置环境**
   这里写入具体的步骤说明。
2. **第二步：导入数据**
   这里写入具体的步骤说明。
3. **第三步：运行部署**
   这里写入具体的步骤说明。
